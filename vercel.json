{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "scholar-insights/dist/client",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/api/ssr?path=$1"
    }
  ]
}
